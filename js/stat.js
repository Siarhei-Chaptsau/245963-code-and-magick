'use strict';

// функция вызывается когда игрок проходит уровень
window.renderStatistic = function (ctx, names, times) {
  var names = ['Siarhei'];
  times.length == names.length;

  this.fillText = function (ctx, names, times) {
    alert('Ура вы победили!\nСписок результатов:');
  }

}
